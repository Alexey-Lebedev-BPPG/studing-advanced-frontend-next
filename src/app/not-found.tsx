'use client';

import { NotFoundPageApp } from '@/pages/NotFoundPage';

// eslint-disable-next-line react/no-unused-prop-types
const NotFoundPage = (props: { error: Error }) => <NotFoundPageApp />;

export default NotFoundPage;
